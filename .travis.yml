sudo: false
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: npm
branches:
  only:
    - master # build master branch only
script:
  - hexo generate # generate static files
deploy:
  provider: pages
  skip-cleanup: true
  github-token: bbdc68104ab3b679fb6d5f69a5c18fa8a526aa0b
  keep-history: true
  on:
    branch: master
  local-dir: public
